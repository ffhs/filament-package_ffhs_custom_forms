export default function dragDropElement(group, element) {
    return {
        group: group,
        element: element,
        parent: false,
        container: false,
        init() {
        }
    }
}
